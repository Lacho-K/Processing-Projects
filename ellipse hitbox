int x = 100;
int y = 200;
int r = 50;

void setup (){
size(700,600);
}

void draw(){
ellipse(x,y,2*r,2*r);
float d = dist ( x , y, mouseX, mouseY);
if(d <= r){
fill(255,0,0);
}
else{
fill(0,255,0);
}
}
